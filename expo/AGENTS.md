# Expo HAS CHANGED

Read the exact versioned docs at https://docs.expo.dev/versions/v56.0.0/ before writing any code.

Allways use npm install to app a npm package. Try to avoid editing the package.json directly (if possible)